<UserControl x:Class="TagShelfLocator.UI.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TagShelfLocator.UI.Views"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:TagShelfLocator.UI.ViewModels"
             d:Background="White"
             d:DesignHeight="600" d:DesignWidth="1080">
    <UserControl.Resources>
        <viewModels:MainViewModelLocator x:Key="viewModelLocator"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <Binding Source="{StaticResource viewModelLocator}" Path="ViewModel"/>
    </UserControl.DataContext>
    <DockPanel Margin="5">
        <StackPanel DockPanel.Dock="Right" Width="150">
            <WrapPanel>
                <TextBlock Text="Connection Status: "/>
                <TextBlock Text="{Binding IsReaderConnected}"/>
            </WrapPanel>
            <Button Margin="2" Content="Start" Command="{Binding StartInventoryAsync}"/>
            <Button Margin="2" Content="Stop" Command="{Binding StopInventoryAsync}"/>
        </StackPanel>
        <Grid Margin="0,0,5,0">
            <ListView ItemsSource="{Binding TagList}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <WrapPanel>
                                <TextBlock Text="EPC: "/>
                                <TextBlock Text="{Binding Path=EPC}"/>
                                <TextBlock Text=" "/>
                                <TextBlock Text="TID: "/>
                                <TextBlock Text="{Binding Path=TID}"/>
                            </WrapPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </DockPanel>
</UserControl>
