<UserControl x:Class="ElectroCom.RFIDTools.UI.Views.ReaderManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vms="clr-namespace:ElectroCom.RFIDTools.UI.Logic.ViewModels;assembly=ElectroCom.RFIDTools.UI.Logic"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.DataContext>
    <Binding Source="{StaticResource readerManagementVML}" Path="ViewModel"/>
  </UserControl.DataContext>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="2*"/>
      <ColumnDefinition Width="1*"/>
    </Grid.ColumnDefinitions>
    <Border Grid.Column="1" Style="{StaticResource Segment}" Margin="4">
      <ListBox ItemsSource="{Binding Readers}">
        <ListBox.ItemTemplate>
          <DataTemplate DataType="{x:Type vms:ObservableReaderDetails}">
            <WrapPanel>
              <TextBlock Text="{Binding DeviceID}" Margin="0,0,4,0"/>
              <TextBlock Text="{Binding DeviceName}"/>
            </WrapPanel>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>
  </Grid>
</UserControl>
